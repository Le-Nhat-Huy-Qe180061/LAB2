
import styled from 'styled-components';

export const WrapperDiscountText = styled.span` 
    color: yellow;
    font-weight: 500;
    font-size: 14px;
 `

 export const WrapperPriceOld = styled.span` 
    color: black;
    font-weight: 500;
    font-size: 14px;
    text-decoration: line-through;
 `